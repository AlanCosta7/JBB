export const ncadastro = state => state.ncadastro
export const loading = state => state.loading
export const listauid = state => state.listauid
export const listamap = state => state.listamap
/*
export const titulo1_5 = state => {
    var listamap = state.listamap
    for (let i = 0; i < listamap.length; i++) {
        const element = listamap[i]
        
    }
    .filter(c => c.data.uid === uid)
}*/